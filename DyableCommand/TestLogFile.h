#include "./TestHitList.h"

// This is the class that is repsonsible for taking the existing
// set of word id's and remapping them to their new word id's.
class CTestLogFile {
public:

	// This takes all the word id's stored previously and 
	// remaps them to their base index.
	void RemapWordIDs() {

	}
};